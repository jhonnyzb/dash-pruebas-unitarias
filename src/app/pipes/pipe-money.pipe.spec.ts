import { PipeMoneyPipe } from './pipe-money.pipe';

describe('PipeMoneyPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeMoneyPipe();
    expect(pipe).toBeTruthy();
  });
});
